# Worklog — 2026-03-05

## Контекст
- Проект: `gbu-anic-web-portal`
- Ветка/коммит на момент отчёта: `b8ba42d` (рабочее дерево не закоммичено)
- Цель работ: поднять окружение, починить запуск сайта, подключить БД, обновить визуал, подготовить Prisma.

## Краткий итог
- Установлены и проверены Node.js/pnpm, Python venv, Docker/Compose.
- Починена dev-сборка (ушли от нестабильного Turbopack на webpack в `dev`).
- Поднят PostgreSQL в Docker, выполнены миграции/seed/синхронизация схемы.
- Добавлены fallback-механизмы для главной и публичных настроек при недоступной БД.
- Обновлены шрифты и арктическая палитра (серебро + сине-ледяные акценты).
- Установлен Prisma, выполнен `db pull`, сгенерирован Prisma Client.
- Публичные маршруты сейчас отдают `200`.

## Что было сделано по этапам
1. **Окружение и зависимости**
- Проверены/установлены `node`, `npm`, `pnpm`.
- Выполнена переустановка JS-зависимостей.
- Создана Python-среда `.venv`, добавлены `requirements.txt` и `requirements-dev.txt`.

2. **Сборка Next.js**
- Исправлена ошибка `@tailwindcss/oxide` (native binding) в dev-режиме.
- Переключение dev-режима на webpack:
  - `dev`: `next dev --webpack`
  - `build`: `next build`
  - `start`: `next start`

3. **Стабилизация рендеринга при отсутствии БД**
- Главная (`src/app/page.tsx`) переведена на безопасный data loader с `try/catch` и fallback.
- Публичные настройки (`src/lib/public-settings.ts`) переведены на fallback defaults при отсутствии `DATABASE_URL`/ошибке БД.

4. **Docker + PostgreSQL**
- Добавлен `docker-compose.yml` с контейнером `anic-postgres` (PostgreSQL 16).
- Для стабильного TCP в Linux используется `network_mode: host`.
- Применены:
  - `pnpm db:migrate`
  - `pnpm db:seed`
  - `pnpm db:push`
- БД заполнена демо-данными, в т.ч. пользователи CMS:
  - `admin@anic.ru / admin123`
  - `editor@anic.ru / editor123`

5. **Prisma**
- Установлены `prisma` и `@prisma/client`.
- Инициализированы файлы Prisma (`prisma/`, `prisma.config.ts`, `.env`).
- Выполнены:
  - `pnpm prisma:pull`
  - `pnpm prisma:generate`
- Добавлен helper-клиент: `src/lib/prisma.ts`.
- Добавлены npm-скрипты:
  - `prisma:pull`
  - `prisma:generate`
  - `prisma:studio`

6. **UI/UX (референсы и новая тема)**
- Обновлены шрифты:
  - Заголовки: `Montserrat`
  - Текст: `PT Sans`
- Пересобрана палитра под арктический стиль:
  - серебристые фоны
  - холодные синие/ледяные акценты
- Обновлены ключевые публичные компоненты:
  - `src/app/globals.css`
  - `src/app/layout.tsx`
  - `src/components/public/PublicHeader.tsx`
  - `src/components/public/PublicFooter.tsx`
  - `src/components/public/AskAI.tsx`

7. **Figma**
- Проверен доступ через MCP.
- Текущий аккаунт MCP: `fatazi.official@gmail.com` (`View` seat).
- Конкретный файл по ссылке не доступен по правам (нужен доступ к файлу для этого аккаунта).

## Текущий статус маршрутов (публичные)
- `200`: `/`, `/about`, `/news`, `/research`, `/media`, `/education`, `/partners`, `/documents`, `/procurement`, `/contacts`, `/knowledge-base`

## Структура проекта (сокращённо)
```text
.
├─ docs/
├─ drizzle/
│  └─ migrations/
├─ prisma/
├─ public/
├─ scripts/
├─ src/
│  ├─ app/
│  │  ├─ (public)/
│  │  ├─ (admin)/
│  │  └─ api/
│  ├─ components/
│  │  ├─ public/
│  │  ├─ admin/
│  │  └─ ui/
│  └─ lib/
│     ├─ db/
│     ├─ ai/
│     ├─ search/
│     └─ prisma.ts
├─ docker-compose.yml
├─ package.json
├─ pnpm-lock.yaml
└─ .env.local (локально, в gitignore)
```

## Изменённые файлы (рабочее дерево)
- `package.json`
- `pnpm-lock.yaml`
- `src/app/globals.css`
- `src/app/layout.tsx`
- `src/app/page.tsx`
- `src/components/public/AskAI.tsx`
- `src/components/public/PublicFooter.tsx`
- `src/components/public/PublicHeader.tsx`
- `src/lib/public-settings.ts`
- `.gitignore`
- `docker-compose.yml` (new)
- `prisma.config.ts` (new)
- `prisma/schema.prisma` (new/updated via introspection)
- `src/lib/prisma.ts` (new)
- `.env` (new, для Prisma CLI)
- `.env.local` (локально, не в git)
- `.venv/`, `requirements*.txt` (локальные окружения/инструменты)

## Команды запуска (рабочие)
```bash
# 1) Поднять БД
docker compose up -d

# 2) Применить схему и сиды (если нужно заново)
DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/anic_portal' pnpm db:migrate
DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/anic_portal' pnpm db:push
DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/anic_portal' pnpm db:seed

# 3) Prisma
pnpm prisma:pull
pnpm prisma:generate
pnpm prisma:studio

# 4) Запуск сайта
pnpm dev
```

## Важные замечания
- `.env*` исключены из git (`.gitignore`), поэтому переменные окружения должны храниться локально/в CI secrets.
- Для интеграции Figma нужно выдать доступ к файлу аккаунту MCP.
- Сейчас в проекте одновременно присутствуют Drizzle и Prisma; это допустимо для постепенной миграции.
